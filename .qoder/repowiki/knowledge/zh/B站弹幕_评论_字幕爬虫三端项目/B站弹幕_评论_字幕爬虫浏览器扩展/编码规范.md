- 所有对外部 API 的请求统一封装在 biliFetch / biliFetchJSON 中，附带固定 UA/Referer/Origin 请求头并在失败时抛出带中文描述的错误信息。
- 任务进度与结果通过统一的 send(type, data) 分发到 'progress'/'info'/'success'/'error'/'done'/'file' 等类型，由 popup 侧 switch(msg.type) 渲染日志或下载按钮。
- 可取消的异步操作通过全局 cancelled 标志配合自定义 sleep(ms) 实现，sleep 内部用 setInterval 轮询并支持 clearTimeout/clearInterval 提前退出。
- WBI 签名流程严格遵循 getWbiKeys → getMixKey → encryptWbi 三步，且 img/sub 密钥缓存 1 小时（3600000ms）避免频繁请求 nav 接口。
- 输出格式选择采用 saveFormat 参数分支（json/csv/txt/srt/ass/lrc），每种格式对应一个 genXxx 生成器，文件名统一以 danmaku_/comments_/subtitle_ 前缀加 bvid 拼接。